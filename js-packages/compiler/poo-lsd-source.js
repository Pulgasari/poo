// poo/compiler/poo-lsd-source.js

// poo.lsd exported as a plain JS string, so it can be imported without a
// bundler or fs access (this package is meant to run inside a browser
// Service Worker - see poo/worker). Keep this file in sync with poo.lsd
// by hand for now; a small build step to auto-generate it is a sensible
// later improvement, not done yet.

export default /* the full poo.lsd text above, as a JSON-escaped string literal */;
